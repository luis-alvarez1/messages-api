type Query {
  messages: [Message!]
}

type Mutation {
  createMessage(input: CreateMessageInput): Message
}

type Message {
  title: String!
  content: String!
  author: String!
}

input CreateMessageInput {
  title: String!
  content: String!
  author: String!
}
